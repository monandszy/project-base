docker exec --user=0 networking-cloudflared cloudflared login

docker cp networking-cloudflared:/root/.cloudflared/cert.pem ./docker/cloudflared/cert.pem

docker exec --user=0 networking-cloudflared cloudflared tunnel --config /etc/cloudflared/conf.yml create app-tunnel

docker exec --user=0 networking-cloudflared cloudflared tunnel route dns app-tunnel monand.tech

docker exec -e TUNNEL_ORIGIN_CERT=/etc/cloudflared/cert.pem --user=0 networking-cloudflared cloudflared tunnel --config /etc/cloudflared/conf.yml run app-tunnel

// -e TUNNEL_ORIGIN_CERT=/etc/cloudflared/cert.pem